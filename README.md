# ProyekDicodingAnalisisData

## Setup environment
```
! pip install streamlit -q
!wget -q -O - ipv4.icanhazip.com
```

## Run steamlit app
```
! streamlit run app.py & npx localtunnel --port 8501
```
